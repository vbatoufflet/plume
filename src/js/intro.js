/**
 * Plume
 * https://github.com/vbatoufflet/plume/
 *
 * Copyright (c) 2013 Vincent Batoufflet.
 *
 * License: BSD, see LICENSE for more details.
 */

$(function () {

/*jshint
    trailing: true,
    browser: true,
    devel: true,
    jquery: true
 */

"use strict";

var $window   = $(window),
    $document = $(document);

// Set application root
// var PLUME_APPROOT = $('meta[name=plume-root]').attr('content') || '';

// Set main objects
var $pane  = $('#pane'),
    $view  = $('#view');
